/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import aplikasi from './aplikasi';

AppRegistry.registerComponent(appName, () => aplikasi);
